using System;

namespace CLASSWORK
{
    class Program
    {
        static void Main(string[] args)
        {
            Students[] students = new Students[5];
            
            Console.WriteLine("Enter the students names and their score");
            for (int i = 0; i < 5; i ++)
            {
                Console.Write("student number " + (i + 1) + " = " );
                student[i] = (Console.ReadLine());
                Console.WriteLine();
                Console.Write("Enter the mathematics score of number " + (i + 1));
                student

            }
        }
    }
}
